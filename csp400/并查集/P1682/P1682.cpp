#include <iostream>

using namespace std;
const int maxn = 260;

int n, m, k, f;

int main()
{
    ios::sync_with_stdio(false);
    freopen("in.txt", "r", stdin);
    cin >> n >> m >> k >> f;

    return 0;
}
